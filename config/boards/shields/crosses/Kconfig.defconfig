if SHIELD_CROSSES_RIGHT

config ZMK_KEYBOARD_NAME
	default "Crosses"

config ZMK_SPLIT_ROLE_CENTRAL
	default y

config ZMK_SPLIT_BLE_ROLE_CENTRAL
	default y

endif

if SHIELD_CROSSES_LEFT || SHIELD_CROSSES_RIGHT

config ZMK_SPLIT
	default y

config ZMK_STUDIO_RPC
	default y if ZMK_STUDIO

if ZMK_DISPLAY

  config I2C
      default y

  config SSD1306
      default y

endif # ZMK_DISPLAY

endif

